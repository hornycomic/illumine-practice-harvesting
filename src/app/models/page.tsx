import { AppShell } from "@/components/AppShell";import { Card } from "@/components/Card";
const models=['Highway RO','Community RO','CNG-led RO'];const categories=['Customer Acquisition','Customer Retention','Facility Experience','Staff Behavior','Dealer Involvement','Operational Discipline','Local Market Leverage','Non-Fuel Revenue Opportunity'];
export default function ModelsPage(){return <AppShell title="Models"><div className="grid gap-5 lg:grid-cols-[420px_1fr]"><Card><h2 className="mb-4 font-semibold">Create model</h2><input placeholder="Model name"/><textarea className="mt-3" rows={6} defaultValue={categories.join('\n')}/></Card><div className="space-y-4">{models.map(m=><Card key={m}><h2 className="font-semibold">{m}</h2><p className="text-sm text-slate-600">Practice categories configured for BPCL harvesting.</p><div className="mt-3 flex flex-wrap gap-2">{categories.map(c=><span className="rounded-full bg-mist px-2 py-1 text-xs" key={c}>{c}</span>)}</div></Card>)}</div></div></AppShell>}
